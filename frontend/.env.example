# Base URL for the triage API.
# Leave empty in development. vite.config.js proxies /api to the backend.
VITE_API_BASE=

# Where the vite dev-server proxy forwards /api requests.
VITE_PROXY_TARGET=http://localhost:8000
